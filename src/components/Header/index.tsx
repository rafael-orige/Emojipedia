import styled from "styled-components";

const HeaderText = styled.h1`
    text-align: center;
    margin: 0;
    padding: 20px;
`;

export default function Header() {
    return <HeaderText>Emojipedia</HeaderText>
}